<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.filmee.myapp.mapper.JoinMapper">

    <insert id="insertUser">
        
		INSERT INTO
	    	fm_user(email, password, nickname, salt)
		VALUES
	    	(#{email}, #{password}, #{nickname}, #{salt})

	</insert>
	
	<select id="selectUserWithEmail" resultType = "int">
		SELECT count(*)
		FROM fm_user
		WHERE email = #{email}
	</select>
</mapper>